Embedidng & Semantic search tool requirement
This tool need to take the mode as imput Baseline or diff. 
- Baseline mode, will generate the embedding for full repo and use that embedding to do the scemantic search. The Embediding tool will also be used by inventory generator step that will generate technology and risk area identification inventory. 
Inventory will look like
- Multi-repo indexing and cross-repo queries
- Blast radius — know what breaks before you touch it
- Route inventory - what routes exist in applictaion and which repo

- Diff mode, it wil load the previous saved embeddings and refresh the embeddings for the change set received as input. Uses blast radius to refresh the embedding for impacted files due to the change. 

This tool will be used by agent to query the tehcnology attributed of files, security attributes of files using the embeddings generated by this tool. 

Include unite test as will when implement. 